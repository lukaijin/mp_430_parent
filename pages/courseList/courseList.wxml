<view class="course-list">
   <navigator-bar title="课程" type="{{1}}"></navigator-bar>
   <view class="course-list-item">
     <yx-course-card wx:for="{{courseList}}" wx:key="{{index + 1}}" info="{{item}}"></yx-course-card>
   </view>
 </view>